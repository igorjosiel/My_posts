import { createGlobalStyle } from "styled-components";

export default createGlobalStyle`
    body{
        margin: 0;
        padding: 0;
        font-family: Poppins;
        background-image: linear-gradient(to right, #306fb3, #1e90ff);
    }
    #root{
        margin:0 auto;
    }
`;